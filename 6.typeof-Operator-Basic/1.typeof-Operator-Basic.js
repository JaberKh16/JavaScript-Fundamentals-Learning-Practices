/*
'typeof' Operator is used to find out the type of variables.
General Syntax:  
        typeof  var_name;
*/

// Declaring 'use strict' statement
'use strict';

// Declaring a variable
let song_name = "abar";
console.log(typeof song_name); // returns the type of the variable which type is 'string'

// Declaring a empty string
let empty_str = "";
console.log(typeof empty_str); // retunrs the type of the which type is 'string'

