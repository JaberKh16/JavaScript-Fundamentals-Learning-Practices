// Declaring 'use strict' statement
'use strict';

// Declaring A Variable
let personal_info = {name:"JK", age:27};
console.log(personal_info); // returns the object property with specified values

// In JS, Null means nothing, it is basically a object datatype
personal_info = null; // nulify the object thus it will get emptied
console.log(personal_info); // this also return the object with no values inside it


// check if the null and undefined is same
let var;
let var_null = null