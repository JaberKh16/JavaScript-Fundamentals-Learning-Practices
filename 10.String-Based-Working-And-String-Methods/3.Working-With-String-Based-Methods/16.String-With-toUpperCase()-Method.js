/*
    String Method: toUpperCase()
    ============================
    str.toUpperCase() method is used to uppercase every single 
    character in a given specified string.
*/

// Declaring 'use strict' statement
'use strict';

// Declaring a string type variable with some uppercase and lowercase mixed values
let msg = "ThiS iS someTHING i donT UsualLY DO.";
let conv_uppercase_msg = msg.toUpperCase(); // uppercasing every single characters
console.log(conv_uppercase_msg);