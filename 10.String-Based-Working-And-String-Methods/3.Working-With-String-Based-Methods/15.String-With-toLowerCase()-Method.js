/*
    String Method: toLowerCase()
    ============================
    str.toLowerCase() method is used to lowercase every single 
    character in a given specified string.
*/

// Declaring 'use strict' statement
'use strict';

// Declaring a string type variable with some uppercase and lowercase mixed values
let msg = "ThiS iS someTHING i donT UsualLY DO.";
let conv_lowercase_msg = msg.toLowerCase(); // lowercasing every single characters
console.log(conv_lowercase_msg);